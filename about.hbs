# Third-Party Notices

GCABB incorporates the third-party software listed below. This file is
generated from the locked Cargo dependency graph with `cargo-about`; it does
not alter the license terms of any dependency. Copyright notices and license
texts are reproduced as discovered in each dependency's distributed source.

The GPUI components from
[`zed-industries/zed`](https://github.com/zed-industries/zed) are pinned to
commit `027cf0def75e5c027504f402a6a6c0dcac11f178`. Only crates whose package
manifests explicitly declare `Apache-2.0` are used. The unmarked
`gpui_shared_string` and `gpui_util` crates are replaced by independent MIT
implementations in this repository.

{{#each licenses}}
## {{name}}

**Used by:**
{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

<pre>
{{text}}
</pre>

{{/each}}
